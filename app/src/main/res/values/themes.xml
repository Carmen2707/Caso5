<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.Caso5" parent="Theme.Material3.DayNight">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.Caso5" parent="Base.Theme.Caso5" />
    <style name="Theme.App.Starting" parent="Base.Theme.Caso5">
        <item name="windowSplashScreenBackground">@color/white</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/spain</item>
        <item name="windowSplashScreenAnimationDuration">300</item>
        <item name="postSplashScreenTheme">@style/Base.Theme.Caso5</item>
    </style>
</resources>